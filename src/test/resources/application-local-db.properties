spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/${TEST_DBNAME}?user=${APP_USER}&password=${APP_PASSWORD}
spring.jpa.hibernate.ddl-auto=validate
